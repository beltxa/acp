# ACP Contract Vectors

This directory is the contract parity layer for ACP protocol behavior.

Authoritative metadata:

- scope classification: `sdks/tests/vectors/SCOPE.md`
- vector manifest: `sdks/tests/vectors/manifest.yaml`

Vector families:

- `amqp/` (authoritative public)
- `mqtt/` (authoritative public)
- `well_known/` (authoritative public)
- `security/` (internal-only)

Public parity claims must be evidence-backed by authoritative public vectors and corresponding conformance outputs.

